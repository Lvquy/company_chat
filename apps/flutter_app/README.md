# Flutter Client Starter

Máy hiện tại chưa cài Flutter SDK nên project này được khởi tạo thủ công ở mức starter.

## Bước tiếp theo

1. Cài Flutter SDK.
2. Chạy `flutter create .`
3. Chép giữ lại thư mục `lib/` hiện tại nếu muốn dùng starter UI này.
4. Chạy `flutter pub get` và `flutter run`.
